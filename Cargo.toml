[workspace]

members = [
    "serialize",
    "serialize-derive",

    "ff-asm",
    "ff",

    "ec",

    # "algebra-benches",
    "bls12-377",
    "bls12-381",
    "bn254",
    "mnt-298",
    # "mnt-753",
    # "poly",
    # "poly-benches",
]

[profile.release]
opt-level = 3
lto = "thin"
incremental = true

[profile.bench]
opt-level = 3
debug = false
rpath = false
lto = "thin"
incremental = true
debug-assertions = false

[profile.dev]
opt-level = 0

[profile.test]
opt-level = 3
lto = "thin"
incremental = true
debug-assertions = true
debug = true
